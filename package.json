{
  "name": "ccg",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c yellow,cyan \"npm run -w server dev\" \"npm run -w frontend dev\"",
    "build": "npm run -w frontend build && npm run -w server build",
    "start": "npm run -w server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
