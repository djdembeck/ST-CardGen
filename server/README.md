# Server

TypeScript Node server that exposes `/api/*` endpoints and talks to backends (KoboldCPP, ComfyUI).

## Dev
```bash
npm install
npm run dev
```

## Build / Start
```bash
npm run build
npm run start
```
